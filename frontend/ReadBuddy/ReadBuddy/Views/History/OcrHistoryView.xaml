<!-- ReadBuddy\Views\History\OcrHistoryView.xaml -->
<UserControl x:Class="ReadBuddy.Views.OcrHistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:ReadBuddy.Views.Controls"
             mc:Ignorable="d" d:DesignHeight="600" d:DesignWidth="800">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Back button row -->
        <StackPanel Orientation="Horizontal" Margin="10" HorizontalAlignment="Right" Grid.Row="0">
            <Button Style="{StaticResource BackButtonStyle}"
        Command="{Binding BackCommand}"
        ToolTip="Back">
                <md:PackIcon Kind="ArrowRight"
                             Width="24"
                             Height="24"
                             VerticalAlignment="Center"
                             HorizontalAlignment="Center"/>
            </Button>

        </StackPanel>


        <!-- OCR viewer -->
        <controls:OcrImageControl x:Name="OcrControl"
                                  Grid.Row="1"/>
    </Grid>
</UserControl>
