<Window x:Class="ReadBuddy.Views.WelcomeView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ReadBuddy.Views.Controls"
        mc:Ignorable="d"
        WindowStyle="None" ResizeMode="NoResize"
        Title="Welcome" Height="150" Width="270"
        Icon="pack://application:,,,/ReadBuddy;component/Icon/icon.png"
        WindowStartupLocation="CenterScreen"
        Loaded="Window_Loaded"
        Background="Transparent">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ReadBuddy;component/Resources/Converters.xaml" />
                <ResourceDictionary Source="/ReadBuddy;component/Themes/WelcomeViewStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>

    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CornerRadius="20" CaptionHeight="0" UseAeroCaptionButtons="False" ResizeBorderThickness="7"/>
    </WindowChrome.WindowChrome>

    <Border Background="{StaticResource AccentBrush}"  MouseDown="Border_MouseDown">
        <Grid>
            <!-- Googly Eyes positioned at the top - friendly watching! -->
            <controls:GooglyEyeControl HorizontalAlignment="Left" VerticalAlignment="Top" 
                                     Margin="30,15,0,0" />
            <controls:GooglyEyeControl HorizontalAlignment="Right" VerticalAlignment="Top" 
                                     Margin="0,15,30,0" />

            <!-- Main content in the center -->
            <StackPanel VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,0,0,30" >
                <ProgressBar Style="{StaticResource LoadingProgressBarStyle}"
                             Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>
                <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap"
                           Style="{StaticResource LoadingTextStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
