<UserControl x:Class="ReadBuddy.Views.Flashcards.FlashcardPreparationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:controls="clr-namespace:ReadBuddy.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <!-- Header -->
            <RowDefinition Height="*"/>
            <!-- Content -->
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <!-- Main area -->
            <ColumnDefinition Width="10"/>
            <!-- Spacer -->
            <ColumnDefinition Width="250"/>
            <!-- Sidebar -->
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="2" Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" >
            <controls:GooglyEyeControl Margin="0,0,50,0" />
            <controls:GooglyEyeControl />
        </StackPanel>

        <!-- 🗺 Title -->
        <Viewbox Grid.Row="0" Grid.Column="0" Stretch="Uniform" MaxHeight="200">
            <TextBlock Text="Flashcard Preparation"
                       FontSize="40"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="20,20,0,10"
                       VerticalAlignment="Center"
                       HorizontalAlignment="Center"/>
        </Viewbox>

        <!-- 🎯 Configuration Panel -->
        <StackPanel Grid.Row="1" Grid.Column="0" Margin="10">
            <TextBlock Text="Select Category:" FontWeight="Bold" Margin="0,0,0,4" FontSize="20"/>
            <ComboBox ItemsSource="{Binding AvailableCategories}" SelectedItem="{Binding SelectedCategory}" Style="{StaticResource ComboBoxFlatStyle}" />
            
            <TextBlock Text="Select Language:" FontWeight="Bold" Margin="0,10,0,4" FontSize="20"/>
            <ComboBox ItemsSource="{Binding AvailableLanguages}"
                      SelectedItem="{Binding SelectedLanguage}"
                      Style="{StaticResource ComboBoxFlatStyle}"/>
            
            <Button Content="Create Glossary" Command="{Binding CreateGlossaryCommand}" Margin="0,10,0,0" Style="{StaticResource ButtonStyle}" />
        </StackPanel>
        <controls:GlossaryHistorySidebarControl Grid.Row="1" Grid.Column="2" ItemsSource="{Binding CombinedItems}" ShowDeleteButton="True" />
    </Grid>
</UserControl>
