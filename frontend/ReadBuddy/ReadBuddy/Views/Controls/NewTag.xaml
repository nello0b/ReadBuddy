<!-- ReadBuddy\Views\Controls\NewTag.xaml -->
<UserControl x:Class="ReadBuddy.Views.Controls.NewTag"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ReadBuddy.Views.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Loaded="UserControl_Loaded">
    <UserControl.Resources>
        <Storyboard x:Key="BackgroundAnimation" RepeatBehavior="Forever" AutoReverse="True">
            <ColorAnimation Storyboard.TargetName="TagBorder"
                            Storyboard.TargetProperty="(Border.Background).(SolidColorBrush.Color)"
                            From="{StaticResource PrimaryColor}" 
                            To="{StaticResource SecondaryColor}"
                            Duration="0:0:2"/>
        </Storyboard>
    </UserControl.Resources>

    <Border x:Name="TagBorder"
            Width="50" Height="25" Margin="5"
            Background="{StaticResource PrimaryBrush}"
            CornerRadius="10">
        <TextBlock Text="New" Foreground="White"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   FontSize="15" FontWeight="Bold" />
    </Border>
</UserControl>
