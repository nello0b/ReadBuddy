<!-- ReadBuddy\Views\Controls\GlossaryHistorySidebarControl.xaml -->
<UserControl x:Class="ReadBuddy.Views.Controls.GlossaryHistorySidebarControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
            xmlns:glos="clr-namespace:ReadBuddy.Models.Glossary"
            xmlns:taskmodels="clr-namespace:ReadBuddy.Models.Tasks"
            xmlns:local="clr-namespace:ReadBuddy.Views.Controls"
             mc:Ignorable="d"
             d:DesignHeight="400" d:DesignWidth="250">

    <!-- Resources -->
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ReadBuddy;component/Themes/SidebarStyles.xaml" />
                <ResourceDictionary Source="/ReadBuddy;component/Resources/Converters.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Template for TaskTracker -->
            <DataTemplate DataType="{x:Type taskmodels:TaskTracker}">
                <Border Margin="10,0,10,10" Style="{StaticResource TaskCardBorderStyle}">
                    <StackPanel Margin="10">
                        <TextBlock Text="{Binding Message}"
                                   FontWeight="SemiBold"
                                   FontSize="13"
                                   Foreground="Black"
                                   Margin="0,0,0,5"/>
                        <ProgressBar Style="{DynamicResource TaskProgressBarStyle}"
                                     Minimum="0" Maximum="100" Height="10"
                                     Value="{Binding Progress}"/>
                    </StackPanel>
                </Border>
            </DataTemplate>

            <!-- Template for Glossary -->
            <DataTemplate DataType="{x:Type glos:Glossary}">
                <Grid Margin="6,6,3,6">
                    <local:NewTag HorizontalAlignment="Left"
                                  Visibility="{Binding LastUpdated, Converter={StaticResource IsNewToVisibilityConverter}}"/>
                    <DockPanel>
                        <Button Style="{StaticResource HistoryCardButtonStyle}" Width="160"
                                Command="{Binding DataContext.GlossaryClickedCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}">
                            <StackPanel>
                                <TextBlock Text="{Binding Category}" FontWeight="Bold" TextWrapping="Wrap"
                                           HorizontalAlignment="Center" TextAlignment="Center"
                                           Foreground="{DynamicResource PrimaryTextBrush}"/>
                                <DockPanel LastChildFill="False">
                                    <TextBlock Text="{Binding Entries.Count, StringFormat={}{0} cards}"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               FontSize="11"
                                               DockPanel.Dock="Left"/>
                                    <TextBlock Text="{Binding LastUpdated, StringFormat={}{0:yyyy-MM-dd HH:mm}}"
                                               Foreground="{DynamicResource SecondaryTextBrush}"
                                               FontSize="11"
                                               DockPanel.Dock="Right"/>
                                </DockPanel>
                            </StackPanel>
                        </Button>
                        <Button Style="{StaticResource ButtonStyle}"
                                Command="{Binding DataContext.DeleteGlossaryCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                CommandParameter="{Binding}"
                                ToolTip="Delete this quiz" Padding="0" Margin="5,5,10,5"
                                Visibility="{Binding RelativeSource={RelativeSource AncestorType=UserControl}, Path=ShowDeleteButton, Converter={StaticResource BoolToVisibilityConverter}}">
                            <md:PackIcon Kind="Delete" Width="20" Height="20"/>
                        </Button>
                    </DockPanel>
                </Grid>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Main Border Container -->
    <Border Margin="0"
            BorderBrush="{DynamicResource PrimaryBrush}"
            BorderThickness="2,2,0,0"
            CornerRadius="20,0,0,0"
            Background="{DynamicResource AccentBrush}">
        <DockPanel Margin="0,0,5,0" LastChildFill="True">
            <TextBlock Text="Glossary History"
                       FontSize="25"
                       FontWeight="Bold"
                       Foreground="{DynamicResource PrimaryBrush}"
                       Margin="10" DockPanel.Dock="Top"
                       HorizontalAlignment="Center" TextAlignment="Center"/>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ScrollViewer.Resources>
                    <Style TargetType="ScrollBar" BasedOn="{StaticResource ModernVerticalScrollBar}" />
                </ScrollViewer.Resources>
                <ItemsControl ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}" />
            </ScrollViewer>
        </DockPanel>
    </Border>
</UserControl>
