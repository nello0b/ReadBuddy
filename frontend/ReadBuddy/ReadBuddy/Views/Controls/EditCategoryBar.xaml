<!-- ReadBuddy\Views\Controls\EditCategoryBar.xaml -->
<UserControl x:Class="ReadBuddy.Views.Controls.EditCategoryBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
             x:Name="Root">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ReadBuddy;component/Themes/EditCategoryBarStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Entire clickable bar is now a Button -->
    <Button Style="{StaticResource CategoryBarStyle}" 
            Command="{Binding EditCommand, ElementName=Root}">
        <Grid Margin="5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" VerticalAlignment="Center">
                <Run Text="Category: "/>
            </TextBlock>

            <TextBlock Grid.Column="1" VerticalAlignment="Center" MaxWidth="200" MaxHeight="40"
                       TextWrapping="Wrap">
                <Run Text="{Binding CategoryText, ElementName=Root}" />
            </TextBlock>

            <md:PackIcon Grid.Column="2"
                         Kind="TagEdit"
                         Width="20"
                         Height="20"
                         VerticalAlignment="Center"
                         Margin="5,0,0,0"/>
        </Grid>
    </Button>
</UserControl>
