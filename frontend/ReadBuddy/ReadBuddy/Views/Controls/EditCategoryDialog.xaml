<!-- ReadBuddy\Views\Controls\EditCategoryDialog.xaml -->
<Window x:Class="ReadBuddy.Views.Controls.EditCategoryDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Edit Category" SizeToContent="WidthAndHeight" ResizeMode="NoResize" WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        Background="Transparent">

    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ReadBuddy;component/Themes/EditCategoryDialogStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CornerRadius="20" CaptionHeight="0" UseAeroCaptionButtons="False"  ResizeBorderThickness="0"/>
    </WindowChrome.WindowChrome>

    <Border Padding="20" Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="10" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <StackPanel>
            <TextBox Text="{Binding SelectedCategory, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
         MaxLength="20"
         Style="{StaticResource DialogTextBoxStyle}" />
            <Border BorderThickness="2" BorderBrush="{StaticResource PrimaryBrush}" CornerRadius="5" Margin="10">
                <Grid Height="150" Margin="5">
                    <ScrollViewer
                  VerticalScrollBarVisibility="Auto">
                        <ScrollViewer.Resources>
                            <Style TargetType="ScrollBar" BasedOn="{StaticResource ModernVerticalScrollBar}" />
                        </ScrollViewer.Resources>
                        <ItemsControl ItemsSource="{Binding Categories}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Content="{Binding}"
                            Style="{StaticResource CategoryButtonStyle}"
                            Command="{Binding DataContext.SelectCategoryCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                            CommandParameter="{Binding}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Command="{Binding CancelCommand}" Style="{StaticResource DialogActionButtonStyle}">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="10">
                        <TextBlock Text="Cancel" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <md:PackIcon Kind="Cancel" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
                <Button Command="{Binding SaveCommand}" Style="{StaticResource DialogActionButtonStyle}" Margin="0">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" HorizontalAlignment="Center" Margin="5">
                        <TextBlock Text="Save" VerticalAlignment="Center" Margin="0,0,5,0"/>
                        <md:PackIcon Kind="ContentSave" Width="20" Height="20" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>

        </StackPanel>
    </Border>
</Window>
