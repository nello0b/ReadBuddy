<Window x:Class="ReadBuddy.Views.Controls.CustomMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message" SizeToContent="WidthAndHeight" ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner" WindowStyle="None"
        Background="Transparent">
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ReadBuddy;component/Themes/MessageBoxStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </Window.Resources>
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="0" CornerRadius="20" CaptionHeight="0" UseAeroCaptionButtons="False" ResizeBorderThickness="0"/>
    </WindowChrome.WindowChrome>
    <Border Padding="20" Background="White" BorderBrush="Gray" BorderThickness="1" CornerRadius="10" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <StackPanel>
            <TextBlock x:Name="TitleText" FontWeight="Bold" FontSize="18" Margin="0,0,0,10" />
            <TextBlock x:Name="MessageText" TextWrapping="Wrap" FontSize="16" Margin="0,0,0,20" Width="300" />
            <StackPanel x:Name="ButtonPanel" Orientation="Horizontal" HorizontalAlignment="Center" />
        </StackPanel>
    </Border>
</Window>
